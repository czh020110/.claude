interface:
  display_name: "Sync Codex Project Config"
  short_description: "Sync the full Codex project config template from the template repo"
  default_prompt: "Use $sync-codex-project-config to sync Codex AGENTS.md, .codex/, skills, and project template files into this project."
policy:
  allow_implicit_invocation: false
