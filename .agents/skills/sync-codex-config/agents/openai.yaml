interface:
  display_name: "Sync Codex Config"
  short_description: "Sync Codex project config from the template repo"
  default_prompt: "Use $sync-codex-config to sync Codex AGENTS.md, .codex/, skills, and project template files into this project."
policy:
  allow_implicit_invocation: false
