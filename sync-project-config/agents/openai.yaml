interface:
  display_name: "Sync Project Agent Config"
  short_description: "Generate platform agent and skill config from the Codex source"
  default_prompt: "Use $sync-project-config to initialize or update this repo: confirm with the user first, let the sync adopt any existing AGENTS.md/CLAUDE.md below the marker, then pass the client platform (codex, zcode, claude, codebuddy, workbuddy, workbuddy-cn, opencode)."
policy:
  allow_implicit_invocation: false
