# 项目记忆

`.project-memory/` 是统一的项目记忆文件夹，长期项目记忆保存在其中。每次开始新任务应先读取相关记忆，再动手。
使用 `read-index-memory` skill 读取全部记忆索引（各主题 `MEMORY.md`），再根据索引按需读取正文。

## 项目记忆索引

`.project-memory/` 采用「索引 + 分块文件」范式：
- 每个主题目录有一个 `MEMORY.md` ，只保存索引，不承载正文。
- 命中索引描述的与任务相关时，必须按需读取对应正文文件，不得仅凭索引摘要推断实现；不得读取与当前任务无关正文。


- 新内容优先归入已有正文文件，只有形成新的独立主题才新建文件；正文文件存在则必须在对应 `MEMORY.md` 有索引行。（skill）
- 不设"默认正文文件""兜底文件"，每个正文文件有明确独立职责，明确可分的类型就分开建文件。(skill)
- 只写真实项目事实，不写教程、模板说明或临时修改记录。(skill)
- 迁移、移除或重构后，不应保留“原版本包含…本版本已移除”之类的记录，除非该事实对后续维护仍有约束作用。(skill)

### 记忆索引路径

- [Documents/MEMORY.md](.project-memory/Documents/MEMORY.md)：用户维护的长期项目背景、需求材料、业务规则和补充说明。默认只读，不主动写入正文，只可由 `collect_update_memory` agent 更新索引。
- [Boundary/MEMORY.md](.project-memory/Boundary/MEMORY.md)：必须遵守的限制、明确不做的事项、设计约束、禁止事项、成功标准与质量底线。开始新功能、调整阶段范围或判断是否偏离 Boundary 前优先读取。
- [Target/MEMORY.md](.project-memory/Target/MEMORY.md)：总体流程目标、核心功能目标、阶段功能范围和验收标准。开始开发新功能、拆分阶段任务或确认当前优先级前优先读取。
- [Environment/MEMORY.md](.project-memory/Environment/MEMORY.md)：依赖工具、运行环境、环境变量、外部服务、路径布局和已知环境坑。环境排查、依赖安装、服务启动失败或迁移运行环境前读取。
- [Commands/MEMORY.md](.project-memory/Commands/MEMORY.md)：安装、启动、训练、测试、构建、评估等可执行命令及其前置条件与常见失败恢复。运行命令前优先读取，避免臆造命令。
- [Design/MEMORY.md](.project-memory/Design/MEMORY.md)：项目架构设计、模块划分、设计理由、损失与训练策略和实验设计（针对已有代码的当前设计，而非未来需求）。架构推进、更新 Design 正文或实现与既有设计出现差异时按需读取。
- [Tools/MEMORY.md](.project-memory/Tools/MEMORY.md)：项目中可复用的非主架构工具/特殊脚本（如可视化工具、统计工具、格式化脚本等）。需要复用某类工具或脚本时按索引读取对应正文。
- [TODO/STEP.md](.project-memory/TODO/STEP.md)：项目长期方向和阶段步骤事实源，默认不随每次提交更新，只做大方向参考。开始记录和开发时读取；空模板时只有用户明确要求才设计完整历程表；非空时历程有变更（方向/阶段调整）可由你直接修改。
- [TODO/TODO.md](.project-memory/TODO/TODO.md)：用户自行维护的待办列表。模型可以读取；用户要求写入 TODO.md 时才写入；完成相关待办时在该列表中直接标记状态（未完成/进行中/已完成），不再单独维护 DONE.md。

## 任务执行流程（MUST）

> 所有开发、修改类任务（新增/修复/重构/配置/测试/文档同步）都需要严格按照以下 4 个执行流程来处理任务（纯咨询、审查、调研请求无需第二步）：

1. **读取记忆**：按任务读取相关 `.project-memory/` 主题正文。
   - 先调用 `read-index-memory` skill 读取全部记忆索引。
   - 按索引描述的覆盖范围、读取条件和关键对象，按需读取对应正文文件。
2. **执行**：
   - 判断是否需要使用 `update_plan` 拆解任务。是，则执行：「update_plan 拆解流程」。
   - 任务是否存在 TODO.md 列表。是，则执行：「TODO 待办处理」。
   - 修改是否涉及外部库/接口/API/SDK，是则见「文档查询流程」：
     * 单/双个接口查询：自主使用技术文档查询工具查询接口。
     * 三个及以上接口查询：使用 docs-research skill 完成查询。
   - 修改代码遵循「代码更新规则」
3. **验证**：完成任务（修改或新增了代码）后必须执行 `post-verify` skill 完成最终验证。
   - 验证结束需确保验证脚本位置在`project-script`下，且索引正确。
4. **沉淀记忆**：按「会话沉淀」和「持续更新」处理。

## update_plan 分解任务

仅用于真正多步骤（通常 3 步以上）或复杂任务；单步任务不建清单。符合条件时，使用 `update_plan` 工具管理执行步骤：

1. 使用 `update_plan` 列出本任务的步骤清单；
2. 按清单顺序推进任务；每完成一步、调整步骤或发现新步骤时，实时更新 `update_plan` 中的进度状态，已完成项立即标记，不遗留未完成项；
3. `update_plan` 只记录本任务的拆解方案和执行步骤，与 TODO.md 无关，无需写入；

## 会话沉淀（每轮收尾，MUST）

>每轮回答或执行完成后，静默审视本轮是否产生需要持久化的记忆，按顺序处理。**不打断用户，完成后不汇报**：

1. **新用户偏好**：工作方式（习惯、约定、粒度...）、协作习惯（语言、深度...）、设计取向（风格、技术栈倾向...）的新表达 → 归入对应主题正文并在该主题 `MEMORY.md` 登记索引。
2. **新设计决定**：方案变更、架构调整、边界调整或打算后续使用的新方向 → 先分析、与用户确认后，再修改对应正文并同步索引。
3. **新长期方向**：新的方向或阶段步骤，或判断当前 STEP 已与任务脱节 → 与用户确认后更新 `.project-memory/TODO/STEP.md`。
4. **记忆冲突**：项目记忆与当前实际情况不一致时（用户提出或执行中发现）, 需及时修正记忆。修正后需简要汇报。

以上记忆更新使用 `update-memory` skill（主模型亲自改，不提交 git）。TODO 状态更新（进行中/已完成）不属于记忆更新，无需使用 skill。无可沉淀内容时直接结束。

## 持续更新（MUST）
>当用户提出新的设计方案、架构调整、边界调整、阶段步骤、记忆冲突、架构方向错误、设计取舍、关键设计选择、或新方向时，由你使用 `update-memory` skill 亲自修改相关设计类记忆文档；**更新须先与用户确认**。

**确认前的分析与提问：**

1. **分析可行性**：不可直接认定用户方案可行。需检查是否与现有架构、阶段目标冲突，是否存在技术风险或依赖问题；必要时先读取 Target、Boundary、Design 确认上下文。
2. **与用户讨论**：询问用户并给出选项，说明可行性结论，指出潜在风险或冲突，明确询问“是否确认采用这个方案”；确认前不得修改文档。若当前环境无法交互，则自行基于已有信息判断处理后继续，并在任务结束时明确汇报本次使用了哪些假设和决策、以及需要用户后续确认的事项。
3. **达成一致后修改**：只修改与该方案直接相关的文档与条目，不顺手改无关内容。
4. **不可行的处理**：方案不可行时说明原因和替代建议；部分可行但有风险时明确指出风险点，由用户决定。

---

## 验证脚本索引

修改类任务完成后必须执行 `post-verify` skill 完成最终验证；相关验证所需脚本放在：`.project-script/` ，此脚本和索引只在任务收尾阶段进行收验时，由 `post-verify` skill 处理，平时无需关注此文件夹。

---

# 代码更新规则

- 适用于项目内所有代码新增、修改、删除、重构场景。
- 适用于与代码变更直接相关的验证、进度同步和 git 提交说明整理。

## 修改前必须确认

当任务涉及代码变更时，必须先确认：

1. 根据任务读取 `.project-memory/` 中的相关项目记忆。
2. 相关代码入口、调用方、被调用方和验证方式。
3. 是否存在用户未提交改动；不得覆盖、回滚或删除用户改动。

---

## 代码简洁原则

避免过度设计和无谓复杂度：
- 能不做就不做，能复用现有实现就复用，标准库、原生能力或已安装依赖能覆盖就不新增依赖；
- 只有一个调用方时不要抽象到多调用方，先理解问题再写最小可用方案；
- Bug 修复要定位根因，不能只改症状；
- 涉及改共用函数时先查所有调用方，在共同路由处一次修好。

简洁并非简陋：
- 不要省略验证、错误处理、安全、可访问性和数据丢失防护，也不要留下不理解的改动。
- 非 trivial 逻辑要留一个可运行检查；
- 实现复杂功能时，不要为了简化而使功能缺失或不满足用户需求。
- 设计UI/前端界面时不要为了代码简洁而使设计简陋。
- 完成迁移、移除或重构后，代码和文档应以最终状态为准，不要残留“这是从旧版迁移/移除”的说明，除非旧行为是当前理解代码所必需的约束。
- 命名、注释和代码不得体现“移除后残留”的历史痕迹：不要用 `*_no_foo`、`*_without_foo` 这类名字表达“这个版本去掉了 XX”，也不要写“原版本包含 XX，本版本已移除/迁移”之类注释；直接使用表达最终能力或意图的名字与注释。历史信息保留在 git 提交记录中，不写进当前代码。

---

## 修改中必须遵守

- 优先复用现有函数、类型、模块和项目约定。
- 不顺手重构无关代码。
- 不改变用户未要求改变的行为。
- 修改公共接口、数据模型、配置或状态流时，必须检查调用方。
- 涉及安全边界、权限、支付、删除、发布、外部服务写入等高风险动作时，先确认再执行。
- 不允许把密钥、token、账号写入接口示例。

## 数据模型规范

- 共享数据结构必须有明确字段含义。
- 字段命名应与项目现有风格一致。
- 新增字段必须说明来源、使用方和默认行为。
- 删除或改名字段前必须检查所有引用。

---
# 架构实现节奏规范

- 不要一次性实现全部长期目标；每次推进应形成一个可验证的阶段成果。
- 如果任务大于一个阶段，先拆解为阶段步骤并更新 `.project-memory/TODO/STEP.md`（只记大方向步骤），再按优先级推进。

---

# Git 提交说明规则

- 用户明确要求创建 git commit（提交代码、保存更改）时才提交，并使用 `git-commit` skill 执行。
- 需要回顾历史代码版本时，优先读取对应 commit 的详细描述。
---


# 文档查询流程

> 涉及外部库、框架、SDK、CLI、云服务、模型 API 或 第三方接口、最新技术文档、官方用法确认、版本差异、迁移说明、Release note、Issue、官方教程。需要联网使用**技术文档**查询工具来确定调用方式。（代码语言语法除外，除非语法有误或不确定）

**技术文档查询工具优先级**:

1. `context7 MCP`：查询库、框架、SDK、API 的当前官方技术文档。
2. 网络搜索工具（tavily/web_search等）：查询官方页面、迁移说明、Issue、Release note 或 context7 不足的信息。

若未发现以上工具，尝试使用 tool_search 尝试获取工具。

只涉及单个或两个技术文档/接口需要查询时，直接使用以上工具查询。涉及 3 个及以上需要查询的技术文档或复杂接口时使用 docs-research skill 调用 docs_research agent 查询，不要直接凭记忆执行任务。
当 subagent 提供的信息无法解决问题时，可以根据其提供的来源自主调用技术文档查询工具进行细节查询。

---

## 必须查文档的场景（MUST）

遇到以下任一情况，不得凭记忆实现：

- 接口不确定。
- 接口疑似过时。
- 接口报错但原因不明。
- 版本升级、迁移、弃用接口替换。
- 新增或修改 SDK 初始化、模型调用、工具调用、认证配置。
- 用户明确要求“联网搜索”“查最新文档”“确认官方用法”。

## TODO 待办处理

- 用户表示需要处理待办中的任务，或没有明确任务上下文但说“继续处理任务/待办”时，询问需要执行 TODO.md 中的哪一项，并给出推荐选项。
- 仅当当前任务属于 TODO 中的某项时更新其状态（`[ ]` 未完成 / `[-]` 进行中 / `[x]` 已完成）；临时任务不写入 TODO、不更新 TODO、不归档。

---
# 自定义提示词说明

⬇︎**下列分割线请勿删除**，若添加自定义提示词，请在`---`分割线下一行再进行添加。

---
