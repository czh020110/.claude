- [ ] [Pending plan/design] (Background: [why it is being discussed]; Decision pending: [what needs to be decided]; Completion criteria: [when it counts as done]; Related scope: [module/file/task])
- [-] [Unifying multi-platform agent config migration] (Background: the same agent/skill prompts were previously maintained separately in each platform directory; Decision pending: whether to treat the Codex config as the single source and have `sync-project-config` generate the other platforms' header-adapted configs; Completion criteria: delete duplicate platform prompts in the repo, complete generation trial runs for the four platforms, and verify loading in real clients; Current status: the unified source and generation script are implemented, pending verification in real clients; Related scope: `.codex/agents/`, `.agents/skills/`, `sync-project-config`, `.claude/`, `.zcode/`, `.codebuddy/`)
