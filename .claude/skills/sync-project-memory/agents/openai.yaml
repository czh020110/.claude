interface:
  display_name: "Sync Project Memory"
  short_description: "Push and pull project memory with git"
  default_prompt: "Use $sync-project-memory to push, pull, or check project memory."
policy:
  allow_implicit_invocation: false
