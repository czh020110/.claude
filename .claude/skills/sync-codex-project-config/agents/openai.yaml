interface:
  display_name: "Sync Project Agent Config"
  short_description: "Sync platform-compatible agent config and project templates"
  default_prompt: "Use $sync-codex-project-config and pass codebuddy for CodeBuddy/WorkBuddy, or the matching platform parameter for other clients."
policy:
  allow_implicit_invocation: false
