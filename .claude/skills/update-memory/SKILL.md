---
name: update-memory
description: 任务进行中或任务结束都可使用，用于实时更新项目记忆。需要更新项目记忆时必须使用此技能，根据当前对话与任务中发现的事实局部更新 `.project-memory/` 记忆正文和记忆索引。
---

# 定位与边界（MUST）

- 本 skill 由你（主模型）**亲自执行**，不调用任何 subagent；也不是全量更新。全量汇总更新走 `update-docs`（用户主动要求时调用，委托 subagent），与本 skill 互不替代。
- **局部更新**：只依据当前对话和任务过程中确认的事实，更新需要更新的主题文件（可新建）；不扫描全部代码，不读取全部记忆，只读取本次涉及主题的 `MEMORY.md` 索引与相关正文。
- **不创建 git commit**：只修改工作区文件；git 提交说明与提交由 `update-docs` 流程负责。
- **排除范围（MUST）**：
  - 不维护 `.project-memory/TODO/`（STEP.md 长期计划、TODO.md 共享待办、DONE.md 已完成归档）——实时更新方式见 CLAUDE.md「会话沉淀」。
  - 不维护 `.project-script/`（验证脚本的创建、复用、清理与 `MEMORY.md` 索引同步）——实时更新方式见 CLAUDE.md「本地验证脚本」节。
  - 不更新 `.project-memory/Documents/`：用户上传文档的主题，正文默认只读；其记忆索引由 `update-docs` agent 同步维护，不由本 skill 更新。

# 触发时机

- 任务进行中或任务结束都可使用：对话中用户提出需要记下来的关键信息（偏好、约束、设计决定、环境事实、命令等），或执行过程中发现可验证的新事实时。
- 无可沉淀内容时直接结束，不新建空文件、不加空索引项。

# 更新流程（MUST）

1. **定位主题**：判断事实归属 `.project-memory/` 哪个大主题（Commands/Environment/Target/Design/Boundary，归属原则见下文）。Documents 由 `update-docs` agent 维护，不在本 skill 范围。
2. **读取该主题 `MEMORY.md` 索引**：通过索引描述判断内容应归入哪个已有正文文件；需要时才读取该正文。不读取无关主题。
3. **写入正文**：
   - 优先归入已有正文文件（同一对象/同一工作流/同一设计问题），更新或补充对应章节，替换过时内容。
   - 归入已有文件不匹配时，按主题语义新建 `<主题名>.md` 并在同轮把索引行写入该主题 `MEMORY.md`。
   - 索引行格式：`- [文档名称](文档名称.md) - 覆盖范围、关键对象及应当读取该文档的任务场景。`
4. **收尾校验**：本次涉及主题内正文文件与索引行一一对应，无断链、无遗漏索引、无孤儿文档；删除过时描述、避免重复事实。

# 记忆范式与格式规则

与 `update-docs` agent 相同的项目记忆范式，摘要如下，冲突时以 `update-docs` agent 提示词为权威全文：

- **索引 + 分块文件**：`MEMORY.md` 永远只保存索引行，绝不承载正文；正文文件存在则必须在索引里有对应行。
- **不建兜底文件**：不创建"其他.md""总览.md""基础操作.md"这类无明确职责的文件；新内容优先归入已有文件，只有形成新的独立主题才新建文件并登记索引。
- **事实归属**：同一事实只有一个权威归属位置；新约束归 Boundary，新实现事实归 Design，可执行方法归 Commands，环境事实归 Environment，不塞入 Target。
- **只写真实项目事实**：不写计划、建议、待确认内容、教程、模板说明、修改历史；不写入密码、令牌、Cookie 等凭证，只记环境变量名称与配置方式。
- **文档格式遵守**：使用用户母语；优先模仿已有正文的章节标题、字段命名、条目格式与层级；空模板按模板样式填充，不自创格式。
- **方案更新需确认**：设计类文档（Boundary/Target/Design）的方案变更先按 Memory Update.md §3.2 与用户确认后再写入；工作方式、命令约定等纯事实偏好可直接记录。

# 与 update-docs 的分工

| 场景 | 使用 |
| --- | --- |
| 对话/任务中发现需要记下来的关键信息，实时局部更新 | 本 skill（主模型亲自改，不提交 git） |
| 用户主动要求的阶段性汇总、全量同步、git 提交说明整理 | `update-docs` skill → `update-docs` subagent |

`.project-memory/Documents/`（用户上传文档）正文默认只读，记忆索引由 `update-docs` agent 同步；本 skill 不更新该主题。
